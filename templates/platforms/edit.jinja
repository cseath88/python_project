{% extends 'base.jinja' %}

{% block content %}

<form action="/platforms/{{platforms.id}}" method="post">
    <div>
        <label for="platform-name">Platform Name:</label>
        <input type="text" id="platform-name" name="platform_name" value="{{platforms.platform_name}}" required autofocus>
    </div>
    <button type="submit">Edit Platform</button>

</form>

{% endblock %}