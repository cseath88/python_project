{% extends 'base.jinja' %}

{% block content %}
<h1> {{ platforms.platform_name }} </h1>

    {% for game in games %}
    <tr>
        <td><a href="/games/{{game.id}}" class="links">{{game.title}}</a></td>
        <td></td>
        <td>Current Stock: {{game.stock_level}}</td>
    </tr>   
    {% endfor %}

{% endblock %}